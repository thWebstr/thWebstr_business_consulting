# Copy this file to .env and fill real SMTP credentials
PORT=3000

# Gmail SMTP settings (use an App Password for SMTP_PASS)
# Steps: enable 2-Step Verification, create an App Password for 'Mail', and paste it below.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=thewebstr25@gmail.com
SMTP_PASS=rmhv ilno lapw fapg

# Destination address (where contact messages should land)
TARGET_EMAIL=thewebstr25@gmail.com

# Notes:
# - Keep this file private; do not commit real credentials to source control.
# - If you prefer a transactional provider (SendGrid/Mailgun), replace the SMTP_* values accordingly.
