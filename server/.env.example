# Copy to .env and fill values
PORT=3000
# SMTP settings - use your transactional email provider credentials
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
# Destination address (where contact messages should land)
TARGET_EMAIL=thewebstr@gmail.com
